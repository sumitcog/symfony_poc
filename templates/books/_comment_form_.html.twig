{{ form_start(form) }}
        
        {{ form_row(form.comment) }}
        {{ form_row(form.createdAt) }}
    {{ form_row(form.book) }}
    {{ form_row(form.user) }}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> Edit Book
        </button>
       
        <a href="{{ path('books_list') }}" class="btn btn-link">
            <i class="fa fa-list-alt" aria-hidden="true"></i> Back to list
        </a>
    {{ form_end(form) }}
    
    
    
